name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: "true"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: aarch64-apple-darwin
            os: macos
            arch: arm64
            bundles: "dmg,app"
          - platform: macos-14
            target: x86_64-apple-darwin
            os: macos
            arch: x64
            bundles: "dmg,app"
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            os: windows
            arch: x64
            bundles: "msi,nsis"
          - platform: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            os: linux
            arch: x64
            bundles: "deb,appimage"

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"

      - name: Install deps (ubuntu)
        if: matrix.os == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev libappindicator3-dev \
            librsvg2-dev patchelf libssl-dev libgtk-3-dev

      - run: pnpm install --no-frozen-lockfile --ignore-scripts

      - run: pnpm build

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2

      - name: Build Tauri (installer + bundles)
        shell: bash
        run: |
          pnpm tauri build --target ${{ matrix.target }} --bundles ${{ matrix.bundles }}

      - name: Package macOS portable (zip .app)
        if: matrix.os == 'macos'
        shell: bash
        run: |
          DIR="src-tauri/target/${{ matrix.target }}/release/bundle/macos"
          APP=$(ls "$DIR"/*.app 2>/dev/null | head -1)
          [ -n "$APP" ] && ditto -c -k --keepParent "$APP" "easytube-macos-${{ matrix.arch }}-portable.zip"

      - name: Package Windows portable (zip release dir)
        if: matrix.os == 'windows'
        shell: pwsh
        run: |
          $src = "src-tauri/target/${{ matrix.target }}/release"
          Get-ChildItem $src -File | Where-Object { $_.Extension -notin '.pdb', '.lib', '.exp' } |
            Compress-Archive -DestinationPath "easytube-windows-x64-portable.zip"

      - name: Package Linux portable (copy AppImage)
        if: matrix.os == 'linux'
        shell: bash
        run: |
          DIR="src-tauri/target/${{ matrix.target }}/release/bundle/appimage"
          IMG=$(ls "$DIR"/*.AppImage 2>/dev/null | head -1)
          [ -n "$IMG" ] && cp "$IMG" "easytube-linux-${{ matrix.arch }}-portable.AppImage"

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: "EasyTube ${{ github.ref_name }}"
          body: |
            ## EasyTube ${{ github.ref_name }}

            A desktop YouTube/Bilibili downloader with MCP server support.

            ### Installer downloads
            | Platform | File |
            |---|---|
            | macOS (Apple Silicon) | `.dmg` |
            | macOS (Intel) | `.dmg` |
            | Windows (x64) | `.msi` |
            | Linux (x64) | `.deb` |

            ### Portable downloads (no install needed)
            | Platform | File |
            |---|---|
            | macOS | `.zip` — extract and run `easytube.app` |
            | Windows | `.zip` — extract and run `easytube.exe` |
            | Linux | `.AppImage` — `chmod +x` and run |

            See [README](https://github.com/Yomisana/EasyTube#readme) for setup instructions.
          draft: true
          prerelease: false
          files: |
            src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg
            src-tauri/target/${{ matrix.target }}/release/bundle/msi/*.msi
            src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe
            src-tauri/target/${{ matrix.target }}/release/bundle/deb/*.deb
            easytube-*-portable.*
          fail_on_unmatched_files: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
